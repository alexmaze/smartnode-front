// import * as api from '../api'
// import * as types from './mutation-types'

export const checkConnection = ({ commit }) => {
  // api.checkConnection = ({ commit }) => {
  //   commit(type.)
  // }
}
